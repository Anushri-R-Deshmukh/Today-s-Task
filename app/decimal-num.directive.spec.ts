import { DecimalNumDirective } from './decimal-num.directive';

describe('DecimalNumDirective', () => {
  it('should create an instance', () => {
    const directive = new DecimalNumDirective();
    expect(directive).toBeTruthy();
  });
});
